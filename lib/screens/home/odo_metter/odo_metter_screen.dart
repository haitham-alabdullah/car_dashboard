import 'package:flutter/material.dart';

class OdoMater extends StatelessWidget {
  const OdoMater({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('odo mater'),
      ),
    );
  }
}
